FROM node:20-alpine
RUN apk add --no-cache openssl
WORKDIR /app
COPY server.js agent.js auth.js ./
COPY web ./web
ENV PORT=8080
EXPOSE 8080
RUN mkdir -p /app/data && chown -R node:node /app/data
VOLUME /app/data
USER node
CMD ["node", "server.js"]
